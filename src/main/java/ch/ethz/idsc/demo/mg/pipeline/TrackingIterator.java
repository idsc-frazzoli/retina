// code by mg
package ch.ethz.idsc.demo.mg.pipeline;

// provides code to run the pipeline repeatedly with varying sets of parameters. Since we have a TrackingEvaluator,
// this allows finding optimal parameter values.
public class TrackingIterator {
  
  TrackingIterator(){
    //..
  }

  private void iterate() {
    // iterate through a set of different params
  }
  
  private void changeParams() {
    // change the PipelineConfig file
  }
}
